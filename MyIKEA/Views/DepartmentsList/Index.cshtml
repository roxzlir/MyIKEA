@model IEnumerable<MyIKEA.Models.DepartmentList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.EmployeeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department.DepartmentName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepartmentListId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepartmentListId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepartmentListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
